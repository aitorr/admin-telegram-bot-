spring:
  application:
    name: admin-telegram-bot
  datasource:
    url: "${DATABASE_URL:jdbc:postgresql://localhost:5432/admintelegrambot}"
    username: "${DATABASE_USERNAME:postgres}"
    password: "${DATABASE_PASSWORD:postgres}"
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

telegram:
  bot:
    # Use environment variable TELEGRAM_BOT_TOKEN or override in application-local.yml (not committed)
    token: "${TELEGRAM_BOT_TOKEN:8441366454:AAEA1OaGxWaF4OPJf9Tr4pjoKgWV4pqmS-I}"
